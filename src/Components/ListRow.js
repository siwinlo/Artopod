import React from "react";

function ListRow() {
  const show = this.props.row;
  return (
    <div>
      <h3>{row.title}</h3>
      <p>{row.description}</p>
    </div>
  );
}
export default ListRow;
